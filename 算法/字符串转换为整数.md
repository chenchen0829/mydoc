## 描述

```
输入一个由数字组成的字符串，把它转换成整数并输出。例如：输入字符串"123"，输出整数123。
```



## 方案

```c
int StrToInt(const char *str)
{
    int n = 0;
    while (*str != 0)
    {
        int c = *str - '0';
        n = n * 10 + c;
        ++str;
    }
    return n;
}
//需要考虑的是空字符，正负数判断，整形溢出。对应整形溢出，直接根据正负数，判断长度，
```

